name: Ontario Elementary Source Review Evidence

on:
  pull_request:
    branches:
      - main
      - feature/ontario-elementary-readiness-monitoring-20260811
    paths:
      - 'curriculum/ontario-elementary/source-discovery.v0.json'
      - 'curriculum/ontario-elementary/source-discovery-amendments.v1.json'
      - 'curriculum/ontario-elementary/source-discovery-additions.v1.json'
      - 'curriculum/ontario-elementary/source-discovery-addition-amendments.v1.json'
      - 'curriculum/ontario-elementary/source-locks/**'
      - 'curriculum/reviews/ontario-elementary/sources/**'
      - 'tools/curriculum_source_lock.py'
      - 'tools/ontario_elementary_source_review.py'
      - 'tests/test_ontario_elementary_source_review.py'
      - '.github/workflows/ontario-elementary-source-review.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify-source-review-evidence:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Check out exact source revision
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'
      - name: Install pinned Python verification dependencies
        run: python -m pip install --requirement requirements-dev.txt
      - name: Verify source review tooling
        run: |
          python -m unittest tests.test_ontario_elementary_source_review -v
          python tools/ontario_elementary_source_review.py verify-directory
      - name: Prove deterministic review target plan
        run: |
          mkdir -p /tmp/axiom-source-review
          python tools/ontario_elementary_source_review.py plan \
            --output /tmp/axiom-source-review/plan-a.json
          python tools/ontario_elementary_source_review.py plan \
            --output /tmp/axiom-source-review/plan-b.json
          cmp /tmp/axiom-source-review/plan-a.json /tmp/axiom-source-review/plan-b.json
          python - <<'PY'
          import json
          from pathlib import Path
          from tools.ontario_elementary_source_review import verify_directory

          plan = json.loads(Path('/tmp/axiom-source-review/plan-a.json').read_text())
          if plan['target_count'] != 16:
              raise SystemExit(f"expected 16 source review targets, found {plan['target_count']}")
          summary = verify_directory()
          if summary['targets'] != 16:
              raise SystemExit(f"unexpected review target count: {summary}")
          if summary['human_source_review_complete'] and summary['approved_sources'] != 16:
              raise SystemExit(f"source review completeness overclaim: {summary}")
          PY
      - name: Upload deterministic review plan
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.0
        with:
          name: ontario-elementary-source-review-plan-${{ github.sha }}
          path: /tmp/axiom-source-review/plan-a.json
          if-no-files-found: error
          retention-days: 7
